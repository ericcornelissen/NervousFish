<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.nervousfish.nervousfish.activities.MainActivity"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:background="@color/default_android_background"
    tools:showIn="@layout/activity_main">

    <ImageView
        android:id="@+id/no_users_icon"
        android:layout_width="@dimen/no_users_icon_size"
        android:layout_height="@dimen/no_users_icon_size"
        android:layout_marginBottom="@dimen/no_users_icon_margin_bottom"
        android:src="@drawable/users_icon"
        android:layout_gravity="center"
        android:contentDescription="@string/your_contacts_list_is_empty"/>

    <TextView
        android:id="@+id/no_users_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textColor="@color/color_accent"
        android:textSize="@dimen/no_users_text_size"
        android:layout_marginBottom="@dimen/no_users_text_margin_bottom"
        android:text="@string/your_contacts_list_is_empty"
        />
</LinearLayout>
